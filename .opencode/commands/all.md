---
description: Run full test suite (30 tests)
---
Run `mvn clean test`. After it finishes, read `target/surefire-reports/testng-results.xml` and show a breakdown by test class — how many passed, failed, skipped, and the total duration.
