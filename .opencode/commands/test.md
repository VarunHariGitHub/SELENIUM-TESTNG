---
description: Run a specific test class, e.g. /test LoginTests
---
Run `mvn clean test -Dtest=$ARGUMENTS` and then read `target/surefire-reports/testng-results.xml`. Give me the pass/fail results for that test class only.
